以下のファイルのUI実装をデザインシステムに照らしてレビューしてください。

確認項目:
1. ハードコードされた色・サイズがないか（CSS変数を使っているか）
2. 禁止フォント（Inter, Roboto, Arial）を使っていないか
3. ダークモード・ライトモードの両方で表示が崩れないか
4. アニメーションの duration が --duration-* を使っているか
5. spacing が tokens.json の値に準拠しているか

対象: $ARGUMENTS（ファイルパスまたは "all"）

"all" の場合は src/components/ 配下の全ファイルを対象にする。

違反を見つけたら：
- 違反箇所（ファイル名・行番号）
- 違反内容
- 修正案
をリストアップしてください。

修正は自動では行わず、レポートのみ出力してください。